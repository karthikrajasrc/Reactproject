## React project setup
-To install = npm create vite@latest
-To install node modules = npm i (or) npm install 
-To open package.json = npm init
-To add the dependecy = npm i package_name
-To add the dev dependecy = npm i -D package_name
